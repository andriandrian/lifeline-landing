{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "pnpm run build",
    "installCommand": "pnpm install",
    "outputDirectory": ".next",
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "dest": "/index" }
    ]
}